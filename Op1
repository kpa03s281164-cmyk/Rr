--// Aim Lock - Roblox Studio
--// LocalScript > StarterPlayer > StarterPlayerScripts

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local camera = workspace.CurrentCamera

--========================
-- SETTINGS
--========================

local enabled = false
local aimPart = "Head"

local fovSize = 150
local smoothness = 0.15
local maxDistance = 500

local target = nil

--========================
-- GUI
--========================

local gui = Instance.new("ScreenGui")
gui.Name = "AimLock"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.Parent = player:WaitForChild("PlayerGui")

-- วงกลางจอ
local circle = Instance.new("Frame")
circle.Name = "FOV"
circle.AnchorPoint = Vector2.new(0.5, 0.5)
circle.Position = UDim2.fromScale(0.5, 0.5)
circle.Size = UDim2.fromOffset(fovSize, fovSize)
circle.BackgroundTransparency = 1
circle.BorderSizePixel = 0
circle.Parent = gui

local circleCorner = Instance.new("UICorner")
circleCorner.CornerRadius = UDim.new(1, 0)
circleCorner.Parent = circle

local circleStroke = Instance.new("UIStroke")
circleStroke.Thickness = 2
circleStroke.Transparency = 0.15
circleStroke.Parent = circle

--========================
-- MENU
--========================

local menu = Instance.new("Frame")
menu.Size = UDim2.fromOffset(125, 85)
menu.Position = UDim2.fromOffset(20, 100)
menu.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
menu.BorderSizePixel = 0
menu.Active = true
menu.Parent = gui

local menuCorner = Instance.new("UICorner")
menuCorner.CornerRadius = UDim.new(0, 8)
menuCorner.Parent = menu

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 18)
title.BackgroundTransparency = 1
title.Text = "Aim Lock"
title.TextColor3 = Color3.new(1,1,1)
title.TextSize = 10
title.Font = Enum.Font.GothamBold
title.Parent = menu

-- ON/OFF
local toggle = Instance.new("TextButton")
toggle.Size = UDim2.new(1, -12, 0, 20)
toggle.Position = UDim2.fromOffset(6, 21)
toggle.TextSize = 9
toggle.Font = Enum.Font.GothamBold
toggle.TextColor3 = Color3.new(1,1,1)
toggle.BorderSizePixel = 0
toggle.Parent = menu

Instance.new("UICorner", toggle).CornerRadius = UDim.new(0,5)

-- FOV
local fovText = Instance.new("TextLabel")
fovText.Size = UDim2.new(1, -12, 0, 15)
fovText.Position = UDim2.fromOffset(6, 44)
fovText.BackgroundTransparency = 1
fovText.TextColor3 = Color3.new(1,1,1)
fovText.TextSize = 9
fovText.Font = Enum.Font.Gotham
fovText.TextXAlignment = Enum.TextXAlignment.Left
fovText.Parent = menu

-- -
local minus = Instance.new("TextButton")
minus.Size = UDim2.fromOffset(25, 18)
minus.Position = UDim2.fromOffset(6, 64)
minus.Text = "-"
minus.TextSize = 14
minus.Font = Enum.Font.GothamBold
minus.TextColor3 = Color3.new(1,1,1)
minus.BorderSizePixel = 0
minus.Parent = menu

Instance.new("UICorner", minus).CornerRadius = UDim.new(0,4)

-- +
local plus = Instance.new("TextButton")
plus.Size = UDim2.fromOffset(25, 18)
plus.Position = UDim2.fromOffset(35, 64)
plus.Text = "+"
plus.TextSize = 14
plus.Font = Enum.Font.GothamBold
plus.TextColor3 = Color3.new(1,1,1)
plus.BorderSizePixel = 0
plus.Parent = menu

Instance.new("UICorner", plus).CornerRadius = UDim.new(0,4)

-- HEAD/BODY
local mode = Instance.new("TextButton")
mode.Size = UDim2.fromOffset(55, 18)
mode.Position = UDim2.fromOffset(64, 64)
mode.Text = "HEAD"
mode.TextSize = 8
mode.Font = Enum.Font.GothamBold
mode.TextColor3 = Color3.new(1,1,1)
mode.BorderSizePixel = 0
mode.Parent = menu

Instance.new("UICorner", mode).CornerRadius = UDim.new(0,4)

--========================
-- UPDATE GUI
--========================

local function updateGUI()
	if enabled then
		toggle.Text = "LOCK : ON"
		toggle.BackgroundColor3 = Color3.fromRGB(45,150,75)
	else
		toggle.Text = "LOCK : OFF"
		toggle.BackgroundColor3 = Color3.fromRGB(60,60,70)
	end

	fovText.Text = "FOV : " .. fovSize
	mode.Text = aimPart == "Head" and "HEAD" or "BODY"

	circle.Size = UDim2.fromOffset(fovSize, fovSize)
end

--========================
-- BUTTONS
--========================

toggle.Activated:Connect(function()
	enabled = not enabled

	if not enabled then
		target = nil
	end

	updateGUI()
end)

minus.Activated:Connect(function()
	fovSize = math.max(50, fovSize - 25)
	updateGUI()
end)

plus.Activated:Connect(function()
	fovSize = math.min(500, fovSize + 25)
	updateGUI()
end)

mode.Activated:Connect(function()
	if aimPart == "Head" then
		aimPart = "HumanoidRootPart"
	else
		aimPart = "Head"
	end

	updateGUI()
end)

--========================
-- ตรวจว่ามีกำแพงไหม
--========================

local function canSee(part)
	if not part then
		return false
	end

	local origin = camera.CFrame.Position
	local direction = part.Position - origin

	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Exclude
	params.FilterDescendantsInstances = {
		player.Character
	}

	local result = workspace:Raycast(
		origin,
		direction,
		params
	)

	if not result then
		return true
	end

	return result.Instance:IsDescendantOf(part.Parent)
end

--========================
-- หาเป้าหมาย
--========================

local function findTarget()
	local bestPlayer = nil
	local bestScore = math.huge

	local center = Vector2.new(
		camera.ViewportSize.X / 2,
		camera.ViewportSize.Y / 2
	)

	for _, other in ipairs(Players:GetPlayers()) do

		if other ~= player then

			local char = other.Character

			if char then

				local humanoid =
					char:FindFirstChildOfClass("Humanoid")

				local part =
					char:FindFirstChild(aimPart)

				if humanoid
					and humanoid.Health > 0
					and part then

					local screen, visible =
						camera:WorldToViewportPoint(part.Position)

					if visible and screen.Z > 0 then

						local screenDistance =
							(Vector2.new(screen.X, screen.Y) - center).Magnitude

						local worldDistance =
							(camera.CFrame.Position - part.Position).Magnitude

						if screenDistance <= fovSize / 2
							and worldDistance <= maxDistance
							and canSee(part) then

							if screenDistance < bestScore then
								bestScore = screenDistance
								bestPlayer = other
							end
						end
					end
				end
			end
		end
	end

	return bestPlayer
end

--========================
-- AIM LOCK
--========================

RunService.RenderStepped:Connect(function()

	if not enabled then
		return
	end

	if not target then
		target = findTarget()
	end

	if target and target.Character then

		local part =
			target.Character:FindFirstChild(aimPart)

		local humanoid =
			target.Character:FindFirstChildOfClass("Humanoid")

		if not part
			or not humanoid
			or humanoid.Health <= 0
			or not canSee(part) then

			target = nil
			return
		end

		-- ล็อกกล้องแบบนุ่ม
		local desired =
			CFrame.lookAt(
				camera.CFrame.Position,
				part.Position
			)

		camera.CFrame =
			camera.CFrame:Lerp(
				desired,
				math.clamp(smoothness, 0.01, 1)
			)
	end
end)

--========================
-- ลากเมนู
-- PC + MOBILE
--========================

local dragging = false
local dragStart
local startPosition

menu.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = menu.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UIS.InputChanged:Connect(function(input)

	if not dragging then
		return
	end

	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

		local delta = input.Position - dragStart

		menu.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)

updateGUI()
